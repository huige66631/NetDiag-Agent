# Windows 网络修复

## 本机侧基础修复
当 DNS 或连接状态异常时，可以尝试重新连接网络、重启网卡、清理 DNS 缓存，或重启路由器。清理 DNS 缓存可使用 `ipconfig /flushdns`。

## 代理和虚拟网卡
代理、游戏加速器、VPN、虚拟机网卡和热点共享可能改变路由路径。诊断前后应记录这些软件是否开启，必要时关闭后对比测试。

## 证据保留
不要只保存截图。更好的证据包括诊断报告、Ping 丢包率、平均延迟、DNS 服务器、DNS 解析耗时和持续监控曲线。
