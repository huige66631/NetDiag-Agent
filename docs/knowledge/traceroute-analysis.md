# Traceroute 路由分析

## 如何看路由路径
Traceroute 用于观察从本机到目标服务的路径跳数和每一跳大致延迟。某一跳偶尔星号不一定代表故障，因为很多路由器会限制 ICMP 回复。

## 判断瓶颈
如果从某一跳开始，后续所有跳的延迟都持续升高，说明瓶颈可能出现在该跳之后。如果只有中间某一跳高，但后续恢复正常，通常不能直接判定该跳故障。

## 单站点慢
只有某个网站慢时，结合 DNS 解析结果、Ping 和 Traceroute 更有意义。可能原因包括目标站点/CDN 节点、运营商互联路径、地域调度异常。
