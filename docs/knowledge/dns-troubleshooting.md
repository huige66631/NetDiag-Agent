# DNS 故障排查

## 域名解析慢或失败
如果 Ping 公网 IP 基本正常，但访问网页经常卡在打开前几秒，重点检查 DNS。常见表现包括 nslookup 超时、解析耗时超过 800 ms、不同网站都出现首次打开慢。

建议对比本机 DNS、公共 DNS 和运营商 DNS。若更换 DNS 后网页首包速度明显改善，问题更可能在 DNS 服务器或本地 DNS 劫持/污染配置，而不是 Wi-Fi 信号。

## DNS 证据表达
反馈给网络管理员时，不要只说“网页慢”。应提供当前 DNS 服务器、解析域名、解析耗时、是否失败，以及同一时间 Ping 公网 IP 是否正常。
